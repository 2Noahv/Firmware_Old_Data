#include <stdio.h>

int main(void)
{
	
  long long int a,b;          //주의:계산의 결과가 int 범위를 넘어가는지를 잘 생각해 보아야 한다
                              //long long int 의 경우 형식 지정자로 %lld 을 사용합니다.
	scanf("%lld %lld", &a,&b);
	printf("%lld",a+b);

	return 0;
}
